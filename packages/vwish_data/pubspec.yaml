name: vwish_data
description: Storage, persistence, session repositories, natural sort, and episode scanner.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  vwish_domain:
    path: ../vwish_domain
  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  path: ^1.9.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
